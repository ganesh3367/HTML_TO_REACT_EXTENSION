# 🌟 HTML to React (JS)

Convert any HTML/CSS snippet into clean React JSX directly inside VS Code!

---

## ⚙️ Features
- 🔄 Converts plain HTML → JSX automatically  
- 🧩 Fixes attributes like `class` → `className`, `for` → `htmlFor`  
- 🎨 Converts inline styles into React style objects  
- ⚡ Converts event handlers (`onclick`, `onsubmit`) → arrow functions  
- 🧠 One-click conversion: Right-click → **Convert to React Component**

---

## 🚀 How to Use
1. Select any HTML code in the editor  
2. Right-click → **Convert to React Component**  
3. Enter a component name (e.g. `MyComponent`)  
4. 💥 Instantly get JSX in a new tab  



## 🧾 Example
**Input HTML**
```html
<div class="card" style="background: red;" onclick="alert('Hi!')">
  <h2>Hello Ganesh</h2>
</div>
